import { Component,inject,signal } from '@angular/core';import { DatePipe,DecimalPipe } from '@angular/common';import { ApiService } from '@shared/api.service';import { Order,ReportSummary } from '@shared/models';
@Component({selector:'app-overview',standalone:true,imports:[DecimalPipe,DatePipe],template:`
<section class="page-heading"><div><p class="eyebrow">Operations</p><h1>Trading Overview</h1><p class="muted">Current warehouse metrics and recent trading activity.</p></div></section>
@if(summary();as s){<section class="metric-grid"><article><span>Trades</span><strong>{{s.trade_count}}</strong><small>Warehouse executions</small></article><article><span>Total notional</span><strong>{{s.total_notional|number:'1.2-2'}}</strong><small>Across loaded trades</small></article><article><span>Active clients</span><strong>{{s.active_clients}}</strong><small>Trading clients</small></article><article><span>Instruments</span><strong>{{s.instruments_traded}}</strong><small>Distinct instruments</small></article></section><div class="data-freshness">Warehouse last loaded <strong>{{s.warehouse_last_loaded_at|date:'medium'}}</strong></div>}
<section class="data-card"><div class="card-heading"><div><h2>Recent operational orders</h2><p>Latest orders available to operations.</p></div></div><table><thead><tr><th>Order</th><th>Side</th><th class="numeric">Quantity</th><th>Status</th><th>Submitted</th></tr></thead><tbody>@for(o of orders();track o.orderId){<tr><td><strong>{{shortId(o.orderId)}}</strong></td><td><span class="side-badge" [class.sell]="o.side==='SELL'">{{o.side}}</span></td><td class="numeric">{{o.quantity|number:'1.0-8'}}</td><td><span class="status-pill">{{o.status}}</span></td><td>{{o.submittedAt|date:'medium'}}</td></tr>}@empty{<tr><td colspan="5" class="empty">No orders available.</td></tr>}</tbody></table></section>`})
export class OverviewComponent{private api=inject(ApiService);summary=signal<ReportSummary|null>(null);orders=signal<Order[]>([]);constructor(){this.api.reportSummary().subscribe(x=>this.summary.set(x));this.api.adminOrders().subscribe(x=>this.orders.set(x.slice(0,10)));}shortId(id:string){return 'ORD-'+id.replaceAll('-','').slice(0,8).toUpperCase();}}
